services:
  pay233-server:
    build:
      context: ./pay233-server
    command: ["-config", "/app/config.example.json"]
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:8080/healthz | grep ok"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
